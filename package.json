{
  "name": "xlm-flow-indexer",
  "version": "0.1.0",
  "private": true,
  "description": "Self-hostable Stellar ledger analytics indexer: Horizon ingestion into SQLite/Postgres with analytical SQL views.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "xlm-flow-indexer": "./src/cli.ts"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "node --test test/**/*.test.ts",
    "cli": "node src/cli.ts",
    "migrate": "node src/cli.ts migrate",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix"
  },
  "dependencies": {
    "better-sqlite3": "^13.0.3",
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/better-sqlite3": "^9.6.0",
    "@types/node": "^24.13.3",
    "@types/pg": "^8.23.1",
    "eslint": "^10.10.0",
    "prettier": "^3.9.6",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.69.0"
  }
}
